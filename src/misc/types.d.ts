/**
 * Please note that the *.d.ts files can only define types, excluding implementation
 */

export type SumArgs = {
  firstArgument: number;
  secondArgument: number;
};

export function multiplyNumbers(x: number, y: number): number;